# Changelog — kustomer-optimization skill

Log of all changes to this skill's files (`SKILL.md`, `references/`, `scripts/`), in chronological
order. Each entry lists the timestamp, the user who made the change, and a description of what
changed. Compiled from git history (`git log --follow -- .github/skills/kustomer-optimization`) and
from session records for changes not yet committed.

| Timestamp | User | Change |
|---|---|---|
| 2026-06-22 11:26:58 +0300 | denisa-ms | commit `241a0ec` ("skill"). Initial skill creation. |
| 2026-06-25 14:21:31 +0300 | hoterdany | commit `565a4c5` ("more investigations"). |
| 2026-06-29 18:12:32 +0300 | denisa-ms | commit `0c519fe` ("readme"). |
| 2026-07-08 06:02:24 -0700 | Dany Hoter | commit `bafe9cf` ("failure buckets"). Added the ~15-bucket `FailureReason` classification guidance (`knowledge/failure-reason-bucketing.md`). |
| 2026-07-09 12:56:36 +0300 | denisa-ms | commit `f84993f` ("investigation"). |
| 2026-07-13 14:47:44 +0300 | denisa-ms | commit `7a90c4e` ("customer asks"). |
| 2026-07-13 14:48:51 +0300 | denisa-ms | commit `b18d0e5` ("chg"). |
| 2026-07-14 08:33:33 +0300 | hoterdany | commit `dfaf66d` ("docs(kustomer-optimization): note ~30-day telemetry retention (Memento excepted)"). |
| 2026-07-14 09:39:19 +0300 | hoterdany | commit `c0bb071` ("customer complaints and new instructions"). |
| 2026-07-16 07:39:22 -0700 | Dany Hoter | commit `8dc6a24` ("ingestion and regional kuskus"). Added `RegionalTracingTargetUrl`-based endpoint selection and ingestion-health/latency guidance (batch interval + processing time + MV age). |
| 2026-07-17 18:48:10 +0300 | Dany Hoter | commit `dd856f5` ("investigations on throttling"). |
| 2026-07-18 08:53:37 +0300 | Dany Hoter | commit `22209ab` ("new instructions and log of instructions"). Added `references/knowledge/investigation-instructions-changelog.md`. |
| 2026-07-18 23:08:06 -0700 | Dany Hoter | commit `1c46742` ("new investigations about throttling"). |
| 2026-07-19 00:38:30 -0700 | Dany Hoter | commit `248afc4` ("organize investigations in folders"). Introduced monthly `Investigations/yyyy-mm/` folder convention. |
| 2026-07-20 15:05:29 +0300 | denisa-ms | commit `f31e3aa` ("chg"). |
| 2026-07-22 04:04:29 -0700 | Dany Hoter | commit `9519f11` ("folders"). |
| 2026-07-28 18:26:23 -0400 | vplauzon | commit `6bab006` ("."). |
| 2026-07-30 00:18:28 -0700 | Dany Hoter | commit `95d99fa` ("investigations"). |
| 2026-08-03 10:27:07 +0300 | denisa-ms | commit `c1dc4ad` ("customer skill"). |
| 2026-08-06 05:09:58 -0700 | Dany Hoter | commit `0b615f1` ("Adding cpu usage"). Added the customer-cluster-optimizer-style CPU-breakdown-by-workload-type query (query/ingest/materialize cores via `.show commands-and-queries`) to `references/new-kuskus-useful-queries.md`, plus a `Kuskus`-database equivalent using `QueryCompletion`/`CommandCompletion`; updated `SKILL.md`, `references/queries.md`. |
| 2026-08-06 (session `f0ad579b`, uncommitted) | Dany Hoter | Extended the cores-breakdown query with an `OtherCores` bucket (CPU not captured by Query/Ingest/Materialize) and a `FreeCores` column (`TotalClusterCores` via `GetCoresFromSKU()` minus the four categories); added a pie-chart variant alongside the existing timechart. Documented the base64-inline chart-embedding convention in `references/knowledge/chart-generation.md` (no `.png` files written into `Investigations/`). |
| 2026-08-08 23:43 UTC (session `f0ad579b`, uncommitted) | Dany Hoter | Added `scripts/Get-KustoToken.ps1` (caches the AAD bearer token per resource in `$env:TEMP`, refreshing only within 5 min of expiry) and `scripts/Invoke-KustoQuery.ps1` (PowerShell equivalent of `query_kusto.py`, returns `PSCustomObject`s, defaults to the `Kuskus` database/cluster) — eliminates the repeated `az` CLI cold-start cost of `query_kusto.py` when an investigation issues multiple queries. Updated `SKILL.md` to document and recommend these scripts over ad-hoc `query_kusto.py` calls. |
| 2026-08-09 00:0x UTC (session `f0ad579b`, uncommitted) | Dany Hoter | Added `scripts/Invoke-HealthCheck.ps1`: runs all 7 seasonal-baseline health-check indicators (see `references/knowledge/health-check-seasonal-baseline.md`) in **3 Kusto round trips** by combining indicators 1/2/3/4/6 into one `let`/`join` query against `KustoMdmMetricsV1` (engine), indicator 5 against the DM cluster (if `-Dm` given), and indicator 7 (ICMs) against `kuskusops/KustoAuto`; classifies PASS/WARN/ALERT per indicator and overall, with "n/a (sparse baseline)" handling for low-traffic clusters instead of false ALERTs. Verified against `TRD-S60F9UYQQV5UMWG9U6` (Emirates) and `KUSKUSWEU`. |
| 2026-08-09 00:0x UTC (session `f0ad579b`, uncommitted) | Dany Hoter | Added `scripts/generate_cores_charts.py`: reusable headless-matplotlib helper that takes cores-breakdown JSON (cluster, total cores, hourly rows) on stdin and returns both the timechart and pie chart as base64 PNGs on stdout — never writes `.png` files, per the established chart-generation convention. |
| 2026-08-09 00:0x UTC (session `f0ad579b`, uncommitted) | Dany Hoter | Wired the CPU cores-breakdown query and chart generation into `Invoke-HealthCheck.ps1` as a 4th round trip, run automatically for every health check (writes results + both base64 PNGs to `$env:TEMP\healthcheck_charts_<Engine>.json`, or `-ChartsOutFile`; `-SkipCharts` to opt out). Extended the console summary to print the latest-hour cores breakdown table. Updated `SKILL.md` (Health check investigation type + Output Format sections) and `references/knowledge/chart-generation.md` to make cores-breakdown charts (timechart + pie) **mandatory** for every health-check report, pointing at the automated script path instead of hand-built charts. |
| 2026-08-09 00:1x UTC (session `f0ad579b`, uncommitted) | Dany Hoter | Ran health-check investigations for TRD-36K8WRCSZMYR3DU5J1 and WIZPRODEU3 using the new tooling; corrected an initial misreading of the TRD-36K8WRCSZMYR3DU5J1 cores-breakdown chart (read only the latest, quiet hour instead of the full 24h trend) and added all KQL queries used to both reports for reproducibility. |
| 2026-08-09 (session `f0ad579b`, uncommitted) | Dany Hoter | Ran a WIZPRODEU3 7-day per-node CPU hotspot analysis (`RoleInstance` dimension on `\Processor(_Total)\% Processor Time`), reporting both 7-day and hourly per-node averages (7-day rollup masked several >80% hourly spikes on 8 distinct nodes) plus a 7-day cores-breakdown pie chart, in `Investigations/2026-08/2026-08-09-wiz-WIZPRODEU3-cpu-hotspot-7d-report.md`. Created this `CHANGELOG.md`, backfilled from `git log --follow` plus session history. |
| 2026-08-09 (session `f0ad579b`, uncommitted) | Dany Hoter | Ported the PowerShell tooling to pure Python for macOS/Linux users without PowerShell: added `scripts/kusto_token.py` (cached-token helper, equivalent of `Get-KustoToken.ps1`), `scripts/invoke_kusto_query.py` (fast query runner with a 401/403 forced-refresh-and-retry, equivalent of `Invoke-KustoQuery.ps1`), and `scripts/invoke_health_check.py` (full 4-round-trip health check with cores breakdown and chart generation via `generate_cores_charts.py`, equivalent of `Invoke-HealthCheck.ps1`). Verified end-to-end against KUSKUSWEU (matching WARN verdict logic) and confirmed token caching (~4.5s repeat runs). Updated `SKILL.md` and `references/knowledge/chart-generation.md` to document the Python scripts as the cross-platform alternative alongside the existing PowerShell tooling. |
| 2026-08-09 02:1x UTC (session `f0ad579b`, uncommitted) | Dany Hoter | Made the Python tooling the **sole** supported query/health-check runner for both Windows and macOS, retiring PowerShell entirely: deleted `scripts/Get-KustoToken.ps1`, `scripts/Invoke-KustoQuery.ps1`, `scripts/Invoke-HealthCheck.ps1`. Rewrote `SKILL.md`'s tooling section ("Faster Python tooling") to present `kusto_token.py` / `invoke_kusto_query.py` / `invoke_health_check.py` as the only supported scripts (no PowerShell fallback, no ad-hoc new scripts), and clarified `invoke_kusto_query.py`'s `run_query()`/CLI is usable for **any** investigation type (health check, throttling, ingestion, capacity, ad-hoc), not just health checks. Updated remaining `.ps1` references in `SKILL.md` and `references/knowledge/chart-generation.md` to point at the Python equivalents. Re-verified `invoke_kusto_query.py` against `KUSKUSWEU` after removing the `.ps1` files. |
| 2026-08-09 02:3x UTC (session `f0ad579b`, uncommitted) | Dany Hoter | Simplified the cores-breakdown query/charts: removed the `TotalClusterCores` (`GetCoresFromSKU()`) calculation and the `FreeCores` column from `build_cores_query()` in `scripts/invoke_health_check.py` — now only Query/Ingest/Materialize/Other cores are computed. Removed the pie-chart generation entirely from `scripts/generate_cores_charts.py` (kept only the timechart, `pie_b64` no longer produced), and dropped the `Free` series from the timechart itself. Updated `SKILL.md`, `references/knowledge/chart-generation.md`, and `references/new-kuskus-useful-queries.md` (removed the standalone "CPU breakdown pie chart" query section and the `TotalClusterCores`/`FreeCores` extensions from the timechart query) to match. Re-verified `invoke_health_check.py` end-to-end against `KUSKUSWEU`: charts JSON now contains only `cluster`, `rows`, `timechart_b64` (no `pie_b64`/`totalClusterCores`). |
| 2026-08-09 (session `f0ad579b`, uncommitted) | Dany Hoter | Added a mandatory-file-creation rule to `SKILL.md`'s Output Format section — every investigation, including quick/targeted checks, must produce a saved `.md` report under `Investigations/yyyy-mm/`, not just a chat answer. Backfilled the missing report for a prior WIZPRODIL1 query-failure check (`Investigations/2026-08/2026-08-09-wiz-WIZPRODIL1-query-failures-report.md`). Ran health checks on `KUSTODATAESTATE`/`KUSTODATAESTATEFW` (+ their DM clusters); found and root-caused a genuine self-resolved query-latency/throttling incident on KUSTODATAESTATE (06:00-07:00 UTC spike, `RequestRateLimitPolicy` throttling + rowstore-unavailable errors), written to `Investigations/2026-08/2026-08-09-msazurecloud-KUSTODATAESTATE-KUSTODATAESTATEFW-healthcheck-report.md`. |
| 2026-08-09 (session `f0ad579b`, uncommitted) | Dany Hoter | Added a new mandatory reporting rule to `SKILL.md` and `references/knowledge/chart-generation.md`: any WARN/ALERT/outlier finding on a time-series metric (query latency, query errors, ingestion latency/volume/failures, CPU, etc.) must include its own timechart of that specific metric (spanning a window wide enough to show the spike/regression), separate from the mandatory cores-breakdown chart — applies to any investigation type, not just health checks. Backfilled a query-latency/failure-rate spike timechart (dual-axis, 48h) into the existing KUSTODATAESTATE/KUSTODATAESTATEFW health-check report to illustrate the pattern. |
